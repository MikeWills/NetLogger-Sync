name: Claude Code Review

on:
  pull_request:
    types: [opened, synchronize, ready_for_review, reopened]
    # Optional: Only run on specific file changes
    # paths:
    #   - "src/**/*.ts"
    #   - "src/**/*.tsx"
    #   - "src/**/*.js"
    #   - "src/**/*.jsx"

jobs:
  claude-review:
    # Optional: Filter by PR author
    # if: |
    #   github.event.pull_request.user.login == 'external-contributor' ||
    #   github.event.pull_request.user.login == 'new-developer' ||
    #   github.event.pull_request.author_association == 'FIRST_TIME_CONTRIBUTOR'

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      id-token: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Run Claude Code Review
        id: claude-review
        uses: anthropics/claude-code-action@v1
        with:
          claude_code_oauth_token: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          plugin_marketplaces: 'https://github.com/anthropics/claude-code.git'
          plugins: 'code-review@claude-code-plugins'
          prompt: '/code-review:code-review ${{ github.event.pull_request.number }} --comment'
          # mcp__github_inline_comment__* (inline findings) and the gh pr comment
          # Bash pattern (the "no issues found" summary comment) are both
          # publish-type actions Claude Code's own permission system requires
          # explicit allowlisting for — without this, --comment above silently
          # has nothing to post with, separately from the workflow's GITHUB_TOKEN
          # permissions block above. Skill must be allowlisted too, or the very
          # first call — invoking code-review:code-review itself — gets denied
          # (observed via show_full_output: true) and Claude falls back to an
          # unstructured, incomplete manual review via raw gh/Bash calls instead.
          claude_args: |
            --allowedTools "Skill,Bash(gh pr comment:*),mcp__github_inline_comment__*"
          # See https://github.com/anthropics/claude-code-action/blob/main/docs/usage.md
          # or https://code.claude.com/docs/en/cli-reference for available options

